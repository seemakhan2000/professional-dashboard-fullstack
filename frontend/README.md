# Dashboard Frontend

Next.js 15 + TypeScript + Tailwind CSS frontend for the professional dashboard.
See the root `README.md` for full setup instructions.

Quick start:

```bash
npm install
npm run dev
```

App runs at http://localhost:3000
