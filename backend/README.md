# Dashboard Backend

Express + MongoDB (Mongoose) API for the professional dashboard.
See the root `README.md` for full setup instructions.

Quick start:

```bash
npm install
npm run dev
```

API runs at http://localhost:6000
